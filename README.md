# onefig

```sh
of [command]

Commands:
  of gen    Generates configuration file artifacts.
  of check  Validates configuration files.

Options:
  --help     Show help [boolean]
  --version  Show version number [boolean]
```
